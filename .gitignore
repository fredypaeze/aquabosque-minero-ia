# Entorno / cachés / secretos (NO se versionan)
venv/
__pycache__/
*.pyc
.pytest_cache/
.env
config/credentials.yaml

# Datos intermedios transitorios
data/interim/*
!data/**/.gitkeep

# NOTA: data/raw, data/processed, models y outputs SÍ se versionan (repo
# self-contained: clonar → correr el dashboard o reconstruir sin conexión).

# Interno / no versionar (agregado por integración capa satelital + IA)
docs/AUDITORIA_CONCURSO_2026.md
tmp/
data/raw/satelital/
outputs/*_entrega_completa.zip
