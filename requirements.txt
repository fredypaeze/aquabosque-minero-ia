# AquaBosque Minero IA — runtime (CPU, sin GPU, sin dependencias geoespaciales pesadas)
# El pipeline y el dashboard corren en cualquier PC: integración municipal por
# centroides + haversine (no requiere geopandas/GDAL). Pinneado a las versiones
# validadas en desarrollo para reproducibilidad exacta.
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.3.0
shap==0.52.0
plotly==6.9.0
streamlit==1.59.1
requests==2.34.2
PyYAML==6.0.3
joblib==1.5.3
